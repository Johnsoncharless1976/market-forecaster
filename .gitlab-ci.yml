# GitLab CI configuration for Zen Market AI
# Runs daily forecast, ingests data, emails results

stages:
  - ingest
  - forecast
  - deploy

# -----------------------
# Stage 1: Data Ingestion
# -----------------------
daily_ingestion:
  stage: ingest
  image: python:3.11-slim
  before_script:
    - pip install -r requirements.txt
  script:
    - python src/data_ingestion.py
  artifacts:
    paths:
      - logs/
    expire_in: 7 days

# -----------------------
# Stage 4: Forecast + Email
# -----------------------
stage4_forecast:
  stage: forecast
  image: python:3.11-slim
  before_script:
    - pip install -r requirements.txt
  script:
    - python src/stage4_forecast.py
    - python src/send_email.py
  artifacts:
    paths:
      - out/forecast.json
    expire_in: 7 days

# -----------------------
# Stage 5: Deployment placeholder
# -----------------------
deploy_prod:
  stage: deploy
  image: alpine:latest
  script:
    - echo "🚀 Deploying Zen Market AI…"

# -----------------------
# Trigger Rules
# -----------------------
# Run on pushes to main and on scheduled triggers
workflow:
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push"'
    - if: '$CI_PIPELINE_SOURCE == "schedule"'
